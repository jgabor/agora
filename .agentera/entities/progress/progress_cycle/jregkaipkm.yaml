id: jregkaipkm
artifact: progress
record:
  timestamp: 2026-08-10 10:37
  type: fix
  phase: build
  what: "bjpfdqcalk: replaced inherited credential environment forwarding with isolated temporary OpenCode auth authority and ran the one additionally authorized corrected-code trial."
  context:
    intent: Close the accepted shell-process-entry credential boundary while preserving provider selection, dry nesting, and final-task lifecycle.
    constraints: Use current checkout and opencode/big-pickle; permit exactly one additional real outer attempt with no retry; retain no secret, raw trial evidence, or remote action.
    unknowns: The unrelated local main worktree remains dirty and was not inspected beyond status and refs. Local main advanced independently before this correction and is now 16 commits ahead of origin/main.
    scope: Temporary auth authority, credential-free OpenCode and shell environments, real loopback instrumentation, one corrected live aggregate, gates, integration, and cleanup.
  discovered: Independent Audit proved the prior bootstrap exported the environment key into OpenCode, so its configured shell inherited the key at process entry before the gate unset it. krzutjlaay therefore records live behavior that preceded this shell-entry correction and is superseded for final executable readiness.
  verified: The bootstrap now receives the selected credential through its descriptor, closes the descriptor, writes exactly one selected-provider entry to an isolated regular temporary auth file with mode 600, clears all credential variables, and execs OpenCode without either credential environment variable. OpenCode 1.18.11 real-loopback instrumentation proved the opencode provider resolved an environment-origin synthetic key from this authority, sent the exact expected authorization, and launched the configured shell with both credential variables absent before the gate cleanup statement. Wrapper and nested Agora remained environment-free. Auth-file fallback and other-provider selection use the same necessary isolated store without extra copies. Normal, clean-failure, planted-leak, INT, and TERM paths reaped children before removing output and temporary auth state. Full provider-free tests passed 85 boundary and 57 analysis checks; the checker passed Bash, ShellCheck 0.11.0, ignored-root hygiene, Mage forwarding (8), and offline analysis (56). Restricted race tests, vet, build without VCS stamping, golangci-lint, workflow parsing, and the unchanged dry integration gate passed. The one additional authorized corrected-code trial used opencode/big-pickle and OpenCode 1.18.11. Result v1 was complete with score 15 from 15 counted calls, one completed qualifying marker at ordinal 15, no post-score calls, and 12 outer model steps. Outer and two nested agents used the selected model; the nested run remained fixed-output, no-context, dry, with four turns. All 14 observed shell entries reported both credential variables absent. Reported outer usage was 60,452 total tokens and zero cost. Checkout provenance, permissions, isolation, no installed-Agora use, no command bypass, source-tree non-write, credential scans, and child cleanup passed. This trial is distinct from the earlier successful process-env-inheriting trial and the historical prelaunch authentication failure. Raw trial output, temporary auth state, generated artifacts, and ignored parents were removed. The complete branch diff remains limited to planned state, TODO, docs, ignore, CI, Mage, and evaluator tooling/tests. Ref-only merge simulation against current main had no conflicts; branch divergence is 8 main-side and 5 branch-side commits. Main and remote refs were not moved by this cycle, and no remote action occurred.
  next: Keep the task in progress without evaluation for independent Audit; preserve both historical progress records unchanged and do not touch the dirty main worktree.
  publication_order: 15
