id: bcimzeorwv
artifact: todo
record:
  severity: annoying
  status: open
  description: Evaluate named profiles after `prime` exists; current `config.yaml` covers defaults but not reusable identities
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: Evaluate named profiles after `prime` exists; current `config.yaml` covers defaults but not reusable identities
      severity: annoying
      status: open
      order: 5
